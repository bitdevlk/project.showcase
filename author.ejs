<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/png" href="/favicon.png">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        crossorigin="anonymous">
    <title>
        <%= author.name %>
    </title>
</head>


<body width="100vw">
    <div
        style=" max-width: 100rem; margin-left: auto; margin-right: auto ; padding-left: 2rem ; padding-right: 2rem; padding-top: 4rem; padding-bottom: 4rem; ">

        <div class="text-center"
            style="margin-left: auto; margin-right: auto; padding-left: 2rem ; padding-right: 2rem; padding-top: 1rem; padding-bottom: 1rem;">

            <div class="lead">
                <h1 class="display-6">
                    Soft Vessel
                </h1>
                <h4>
                    React based industrial training
                </h4>
            </div>

            <% if ( author.member) { %>
                <h1 class="display-1">
                    <%= author.member.name %>
                </h1>
                <% } %>
        </div>
        <div>
            <% if ( author.member) { %>

                <a href="<%=author.member.url%>" style="text-decoration: none; color: black;">
                    <%- include('member', {member: author.member}); %>
                </a>
                <% } %>

        </div>

        <% if (author.projects && author.projects.length && author.projects.length !=0) { %>


            <div class="text-center"
                style="margin-left: auto; margin-right: auto; padding-left: 2rem ; padding-right: 2rem; padding-top: 6rem; padding-bottom: 1rem;">

                <h1 class="display-3">
                    Projects
                </h1>

            </div>

            <div
                style="min-width: 40rem; max-width: 80rem; margin-left: auto; margin-right: auto; padding-left: 2rem ; padding-right: 2rem;">

                <%- include('showcase', {projects: author.projects}); %>

            </div>

            <% } %>

    </div>

</body>

</html>